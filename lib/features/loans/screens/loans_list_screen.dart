import 'package:flutter/material.dart';

class LoansListScreen extends StatelessWidget {
  const LoansListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Loans list — Placeholder'),
    );
  }
}
